# Core dependencies
setuptools>=80.9.0  # Security: Fixed PYSEC-2022-43012, PYSEC-2025-49
flask==3.1.2
flask-wtf==1.2.2
flask-limiter==3.12
flask-talisman==1.1.0
flask-login==0.6.3
python-dotenv==1.2.1
requests==2.32.5

# Google Sign-In (JWT verification)
google-auth==2.41.1

# AI/ML
google-generativeai==0.8.5

# Data handling
pydantic==2.12.3

# Database
sqlalchemy==2.0.44
psycopg2-binary==2.9.11
alembic==1.14.0  # Database migrations

# Performance & Caching
redis==5.3.1

# PDF Generation
reportlab==4.4.4

# Security - Content Sanitization
bleach==6.1.0  # XSS protection for user-generated content

# Testing
pytest==7.4.4
pytest-cov==4.1.0

# Security - Dependency Scanning
pip-audit==2.9.0  # Latest version with enhanced OSV database support

# Security Status:
# ✅ All package vulnerabilities resolved (upgraded to latest secure versions)
# ⚠️  pip 25.2 has GHSA-4xh5-x5gv-qwph (tarfile path traversal vulnerability)
#     - Fix planned for pip 25.3 (not yet released as of 2025-10-23)
#     - Risk accepted: LOW impact with mitigations
#     - Mitigations: Only install packages from trusted sources (PyPI)
#     - Update to pip 25.3 when available
